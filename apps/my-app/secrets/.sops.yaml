---
creation_rules:
  # Encrypt the data section of Kubernetes secrets generated from password.txt
  - encrypted_regex: "^(data|stringData)$"
    # Replace with your age public key after running scripts/03-configure-sops.sh
    age: age1...
